# FIRE – Documentation

> **This file is outdated (CSV-era notes). The live documentation is in `DOCUMENTATION.html`.**
>
> In the app: open the **ℹ️ Info** page — it renders `DOCUMENTATION.html` directly.
>
> To read locally: open `DOCUMENTATION.html` in any browser.

The HTML documentation covers:

1. What this app is
2. Tech stack
3. Project structure + all Firestore collections (including `meta/stats`)
4. Firestore data model — every field in every collection
5. Core concepts — ETF types, signals, suggestion algorithm, weighted-avg price, holdingDays
6. Kotak charge rates and examples
7. Application flow — startup, buy, sell (with stats update steps)
8. Page-by-page walkthrough (all 8 pages)
9. Key functions in `data_manager.py` and `app.py`
10. How to run locally + Streamlit Cloud deploy
11. How to extend the app
12. Known simplifications
